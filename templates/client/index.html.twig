{% extends 'base.html.twig' %}

{% block title %}Clients{% endblock %}

{% block body %}
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-800 dark:text-white">Nos Clients</h1>
        <a href="{{ path('app_client_new') }}" class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded shadow hover:shadow-lg transition transform hover:-translate-y-0.5 flex items-center gap-2">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
            </svg>
            Ajouter un client
        </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for client in clients %}
            <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-md border-l-4 border-green-500 hover:shadow-xl transition-shadow duration-300 flex flex-col justify-between">
                
                {# Contenu de la carte #}
                <div class="mb-4">
                    <h2 class="text-xl font-bold text-gray-800 dark:text-white mb-3">{{ client.firstname }} {{ client.lastname }}</h2>
                    
                    <p class="text-gray-600 dark:text-gray-300 mb-2 flex items-center gap-3 text-sm">
                        <span class="p-1.5 rounded bg-gray-100 dark:bg-gray-700 text-gray-500 dark:text-gray-400">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                            </svg>
                        </span> 
                        {{ client.email }}
                    </p>
                    
                    {% if client.phoneNumber %}
                    <p class="text-gray-600 dark:text-gray-300 mb-2 flex items-center gap-3 text-sm">
                        <span class="p-1.5 rounded bg-gray-100 dark:bg-gray-700 text-gray-500 dark:text-gray-400">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                            </svg>
                        </span> 
                        {{ client.phoneNumber }}
                    </p>
                    {% endif %}

                    {% if client.address %}
                    <p class="text-gray-600 dark:text-gray-300 flex items-start gap-3 text-sm">
                        <span class="p-1.5 rounded bg-gray-100 dark:bg-gray-700 text-gray-500 dark:text-gray-400 mt-0.5">
                            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                            </svg>
                        </span>
                        <span>{{ client.address }}</span>
                    </p>
                    {% endif %}
                </div>


                <div class="flex justify-end items-center mt-2">
                    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}
                        <a href="{{ path('app_client_edit', {'id': client.id}) }}" 
                        class="flex items-center px-4 py-2 rounded-lg text-gray-400 hover:bg-gray-700 hover:text-white transition-all duration-200 hover:translate-x-1 text-sm font-medium no-underline">
                        <svg xmlns="http://www.w3.org/2000/svg" style="width:1.25rem; height:1.25rem; margin-right:0.5rem;" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
                        </svg>
                        Modifier
                        </a>
                    {% endif %}
                    
                    {% if is_granted('ROLE_ADMIN') %}
                        <form method="post" action="{{ path('app_client_delete', {'id': client.id}) }}" style="margin-left: 1.5rem;" onsubmit="return confirm('Supprimer ce client ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id) }}">
                            <button class="flex items-center px-4 py-2 rounded-lg text-gray-400 hover:bg-gray-700 hover:text-white transition-all duration-200 hover:translate-x-1 text-sm font-medium no-underline">
                                <svg xmlns="http://www.w3.org/2000/svg" style="width:1.25rem; height:1.25rem; margin-right:0.5rem;" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                </svg>
                                Supprimer
                            </button>
                        </form>
                    {% endif %}
                </div>
            </div>
        {% else %}
            <div class="col-span-3 text-center py-12">
                <div class="inline-block p-4 rounded-full bg-gray-100 dark:bg-gray-700 text-gray-400 mb-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                    </svg>
                </div>
                <p class="text-gray-500 dark:text-gray-400 text-lg">Aucun client trouvé.</p>
                <p class="text-gray-400 dark:text-gray-500 text-sm">Commencez par en ajouter un nouveau.</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}